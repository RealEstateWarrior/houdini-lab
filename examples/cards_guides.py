"""手順ページ用の数字の図。数字はすべて、その手順を作ったときの実測と実験記事から。

    python examples/cards_guides.py
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from cards_058_060 import card  # noqa: E402


def main():
    card(
        "guide_cache_card.png",
        "キャッシュを挟むと — 煙 40フレーム",
        "手順のシーンで測った値。読むほうは計算の約17倍速い。",
        (["やったこと", "時間", "ファイル"], [18, 520, 760]),
        [
            ["計算しながら 40フレーム回す", "0.99〜1.01秒", ("—", "dim")],
            ["Save to Disk で書き出す", "0.15秒", "40個 / 63.3 MB"],
            ["Load from Disk で読みながら回す", ("0.06秒", "ok"), ("—", "dim")],
            ["範囲が式のまま書き出した（実験025）", ("28.81秒", "ng"),
             ("240個 / 3,578.9 MB", "ng")],
        ],
        "書き出しが 0.15秒で済むのは、直前に全フレームを計算済みだったから。",
    )

    card(
        "guide_foreach_card.png",
        "面ごとに高さを変える — for-each と VEX",
        "上流を少し動かして計算し直させたときの時間（7回の中央値・2回測って同じ傾向）。",
        (["面の数", "for-each", "VEX", "差"], [18, 260, 520, 760]),
        [
            ["49面", "2.03 ms", ("0.03〜0.04 ms", "ok"), "58〜68倍"],
            ["441面", "18.23〜18.58 ms", ("0.29〜0.34 ms", "ok"), "53〜65倍"],
            ["3,969面", ("167.20〜167.44 ms", "ng"), ("0.81〜0.87 ms", "ok"), "193〜207倍"],
        ],
        "結果は3通りとも完全に一致。for-each は面の数にほぼ比例して遅くなる。",
    )

    card(
        "077_read.png",
        "実験077 — 15,591粒の位置を1フレーム読む時間",
        "MPM の動く板（40フレーム）。計算そのものは 1フレーム約 86ms。",
        (["読み方", "1フレーム", "40フレーム", "計算と比べて"], [18, 470, 690, 900]),
        [
            ["1粒ずつ position() と v", ("226.34 ms", "ng"), "9.05 秒", ("2.6倍", "ng")],
            ["1粒ずつ position()", ("81.67 ms", "ng"), "3.27 秒", ("0.95倍", "ng")],
            ["pointFloatAttribValues", "0.57 ms", "0.023 秒", "0.7%"],
            ["numpy.frombuffer（文字列で受け取る）", ("0.11 ms", "ok"), ("0.004 秒", "ok"), ("0.1%", "ok")],
        ],
        "位置だけを読んだ3通りは、読んだ値（高さの平均 0.205530）が6桁まで同じ。",
    )


if __name__ == "__main__":
    main()
