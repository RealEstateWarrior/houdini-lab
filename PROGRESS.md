> **古い記録（2026-09-14 時点）。現状は STATE.md を見る。この先は読まなくてよい。**

# いまどこまで進んでいるか

作業が中断しても、このファイルを読めば続きから再開できる。
**1区切りつくごとに必ず更新してコミットする。** 会話の記憶ではなくこのファイルを正とする。

最終更新: 2026-09-14

---

## 現在の状態

- 実験: 001〜090 完了（030・060・090 は振り返り点検）
- 050 から新分野 APEX / リグ に入った
- 手順ページ: 15本（グルーム・HeightField地形・模様・リグ・MPM を追加）
- サイト: 白基調デザイン / ロゴ / ノード解説タブ / 実験ポップアップ / 手順ポップアップ /
  スクロールの動き / 先頭に戻るボタン / スマホ全画面メニュー / 検索窓
- 外部リンクは別タブで開く

## いまの作業（★の4件 → 手順ページ → 実験再開）

| # | やること | 状態 |
|---|---|---|
| S1 | 実験からノード解説とシーンファイルへ飛べるようにする | 完了 |
| S2 | 実験ログをモデリング編とエフェクト編に分割 | 完了 |
| S3 | 概要タブの先頭に「はじめての人へ」の順路 | 完了 |
| S4 | 手順ページ（実験008「地面に物を生やす」） | 完了 |
| S5 | 実験021（煙が動いていなかった件）| 完了 |
| S6 | タグと絞り込み | 完了 |
| S7 | 手順ページ10本（モデリング5・エフェクト5）| 完了 |
| S8 | 実験022（表面を張る。019の係数を訂正）| 完了 |
| S9 | 実験023以降を続ける | 090まで完了（点検済み）。091以降の方向はユーザーと相談して決める |
| S10 | 001〜030 の振り返り点検（実験030）| 完了 |
| S12 | 031〜059 の振り返り点検（実験060）| 完了（6/6 一致） |
| S13 | 振り返り点検 | 090で061〜090を実施（7件測り直して全部一致）。次は120 |
| S11 | 手順ページを全実験分そろえる（いまは15本） | 進行中 |

## サイト改修（2026-09-14 依頼）

親子構成: **Saito Production**（親・`sp.html`）の下に **Houdini 研究部**（今のサイト）。
ロゴは SP のモノグラム（文字なし・押すと親へ）。ナビは1段。部の切り替えは親ページで。

| # | やること | 状態 |
|---|---|---|
| R0 | サムネ70件作り直し（`make_thumbs.py`）・058〜060に図・遅延読み込み・検索に手順 | 完了 |
| R1 | SPロゴ・親ページ `sp.html`・ナビ1段化 | 完了 |
| R2 | ★ユーザー: 新リポジトリ `RealEstateWarrior.github.io` を作る（gh が無いので手作業） | 待ち |
| R3 | ノード画面の撮影。**撮影は完了**（全体 14枚 `out/guide_<id>_ui.png`・各段 78枚 `out/guide_<id>_p<段>.png`、対応表 `out/guide_parm_map.json`）。「模様」だけシーンが無く未撮影。`crop_ui_shots.py` で切り出し、手順ページに組み込み済み（冒頭にネットワーク全体、各段に開閉式のパラメータ画面） | 完了（「模様」だけ残り） |
| R4 | あいまい検索（`search_synonyms.json` 33グループ・表記ゆれ・本文・文の近さ）。親も含めて全体を検索 | 完了 |
| R5 | Artifacts 版に AI チャット（`sample`。検索画面の「AI に聞く」。根拠は検索上位10件） | 完了（実機での動作確認はユーザー待ち） |
| R6 | 概要ページ「これまでに作ったもの」をリンク化。`build_site.py` の `SHOWCASE` で11枚（実験を読む／作り方を見る） | 完了 |
| R7 | 効率化タブ（`speed_tips.json`。5分類20項目、元の実験へボタン、検索にも入る）・以後の実験は時間を必ず記録 | 完了 |
| R8 | 手順ページを 15 → 25 本へ（キャッシュ・粒に風・布を破る・炎と色・毛を風で・草を流れに・動く板で運ぶ・Karma・書き出し・for-each→VEX）。作り方は `examples/guide_cache.py` / `guide_more.py`、数字は各手順のシーンでその場で測定 | 完了（新10本の Houdini 画面も 2026-09-19 に撮影済み） |
| R9 | Artifacts 全ページに反映。親の Artifact は https://claude.ai/artifact/1ayTdyUEY1avfBfUARaiLo | 完了 |

## 済んだことの詳細

- 実験ログは `log.html`（モデリング編 001〜012）と `log_fx.html`（エフェクト編 013〜）の2本。
  古い `#expNNN` リンクは、その編に無ければ正しい編へ自動で転送する
- エフェクト編の Artifact URL は `087548d5-0c09-40b5-8e2e-7bfc084e1f0c`
- 手順ページは `guides.json` が元データ。画像は `examples/guide_*.py` が作る
- 各実験に「使ったノード」と「シーンファイル」の行を自動で差し込んでいる
  （`inject_experiment_links`）

## 再開するときの手順

1. `git -C D:\Claude\houdini log --oneline -5` で直前のコミットを見る
2. このファイルの表で「これから」の一番上から再開する
3. 迷ったら `ROADMAP.md`（何をいつやるか）と `BACKLOG.md`（思いつき）を見る

## hython の癖（踏んだら思い出す）

- **1プロセスで何度もシーンを作り直すと固まる。** 実験022以降、ケースごとにプロセスを分けている
- **レンダしたあとにシーンを作り直すと固まる。** 実験027で10分待っても進まなかった。
  レンダはプロセスの最後に1枚だけ。比較の2枚目は別プロセスで撮り、
  カメラをそろえるため枠を JSON に残して次のプロセスが読む
- 重いシミュレーションは `hou_tools.cache_sim()` を通す（実験027）。
  上流の指紋が変われば自動で計算し直すので、古いキャッシュを読む事故が起きない
- **シーンを作り直すと、前の `hou.Geometry` は死ぬ。** `ObjectWasDeleted` が出る。
  比較で使う数字は、作り直す前にその場で取り出しておく（実験043）

## グルームの癖（実験042・043）

- `fur` の出力は `N` と `P` だけ。`skinprim` が無いので `hairclump` に渡せない
- 土台には `rest` が必要（無いと「Skin input has no rest attribute.」）
- **入力の順番が2つのノードで逆。** `hairgen::2.0` は 土台→ガイド、
  `hairclump::2.0` は 毛→土台。逆にすると `Invalid source` で何も出ない
  （入力ラベルは `node.inputLabels()` で読める。推測しないこと）
- **wrangle の `addpoint` で作った点は `rest` が (0,0,0) のまま。**
  `hairgen` は rest 空間で距離を測るので、ガイドが1本も使われない。
  エラーも警告も出ない。見分け方は毛の長さが `unguidedlength` の既定 0.05
  ちょうどになること（実験044）
- `influenceradius` の既定 0.05 は小さい。ガイド300本・半径1の球で 18.9% しか
  拾えない。0.20 で 100%
- `thickness`（毛の太さ）は**直径**。既定 0.001 は半径1の球を画面いっぱいに
  撮ると 0.103px にしかならず、点の集まりにしか見えない（実験045）

## MPM の癖（実験061〜066）

- **`mpmsolver` は既定で y = 0 に地面を持っている**（`groundactive` = 1）。
  コライダを一つも置かなくても粒は y = 0 で止まる。
  自由落下を測りたいときは切る（実験066）。
  062〜065 で置いた地面の箱は上面がちょうど y = 0 なので、結果には影響しない
- **`materialpreset` はスクリプトから効かない。** 10通り試して9桁まで同一。
  実際に効くのは `materialtype`（elastic / chunky / liquid / viscous / sandy）（実験062）。
  理由は `recipeutils.setPresetMenu` というコールバックで作られているため。
  **`parmTemplate().scriptCallback()` が空でないパラメータは、`parm().set()` だけでは効かない**
- **`materialtype` の既定は 1（Chunky）。** Elastic（0）は跳ね返る。
  同じ落とし方でも30フレーム目の高さが Elastic 1.0462 / Chunky 0.6070
- **`windvelocity` だけ上げても何も起きない。** 風は空気抵抗を通してしか効かない。
  `airdrag` の既定は 0（実験066）
- **`airdrag` は速さの2乗に比例する抵抗。** しかも3次元の相対速度の大きさで決まるので、
  横風が落ちる速さまで変える。終端速度は √(g/d)（実験066）。
  POP の `popwind` の `airresist` も二乗抵抗だった（実験029）
- `mpmsurface` の `outputtype` は既定が Surface VDB。面が欲しいなら `polygonmesh` にし、
  出てくる polysoup を `convert` で poly に直す（実験063）
- **`groundfriction` はクーロン摩擦の μ そのもの。** 止まるまでの距離は 初速²÷(2μg) に
  0.04% で乗る。ただし摩擦が強い（0.5以上）と塊が潰れて広がり、式から外れる（実験067）
- **`groundsticky` は横滑りにはほとんど効かない。** 効くのは「離れるとき」で、
  跳ね返る高さが半分になる（実験067）
- **摩擦の式に乗るのは、形が崩れない材質だけ**（Elastic / Chunky）。
  崩れる順（潰れ具合 0.99 / 1.02 / 7.66 / 91.7 / 243.3）と式から外れる順が完全に一致する（実験068）
- **Liquid は摩擦 1.0 で発散した。** 高さの平均が動かないのに最大だけが伸び続ける。
  **平均と最大を両方測らないと、暴れを結果として書いてしまう**（実験068）
- **計算する範囲（mpmcontainer の大きさ）は結果を変えない。**
  奥行き 8.0 と 16.0 で距離が6桁まで一致（実験067・068）
- **`mpmcollider` の摩擦は組み込みの地面と同じもの**（5通りで差 0.6% 以内）。
  **ただし同じ高さに重ねると、コライダの摩擦は完全に無視される。**
  効くのは「先に触った面」だけ。床を自分で置くなら `groundactive` を 0 にする（実験069）
- **`mpmcollider` の `type` は既定が Static。** 板を動かしても粒は何も感じない。
  `Animated (Rigid)`（1）に変えて初めて動く。エラーも警告も出ない（実験070）
- **`computevelocity` は Rigid では無関係、Deforming では必須。**
  Deforming で切ると、Static とまったく同じ値になる（実験070）
- **「黙って無視されるパラメータ」は5件**（044のガイド / 050の可変長入力 /
  062の materialpreset / 066の風速 / 070のコライダ type）。
  見つけ方はいつも「入力を極端に振って出力が動くかを見る」
- 粒を細かくするほうが substep を細かくするより効くし安い。
  粒24.7倍で時間3.73倍、substep 16倍で時間25.5倍（実験064・065）

## 画を測るときの癖

- **Apprentice のレンダには右下に Houdini のロゴが焼き込まれ、透明度も持っている。**
  何も考えずに数えると測定値に混ざる。600×600 なら箱は
  行 521〜566・列 343〜566、透明度の合計 4163.3。
  実験045で 3〜4% の誤差になり、046 でも同じ罠に掛かった。
  面積・幅・色を測る前に、この箱を 0 にする
- **色を測るときは光を当てない。** 周囲の光が全チャンネルに同じだけ乗るので、
  割合が 0.5 側へ寄る。発光（emitcolor_usePointColor）にすると
  色そのものが測れる（実験046で R² 0.991 → 0.99991）
- sRGB のまま割合を取らない。線形に戻してから測る

## PowerShell の癖

- **`Get-Content -Raw` / `Set-Content` で日本語ファイルを書き換えない。**
  既定のエンコーディングが UTF-8 ではないので文字化けする（2026-09-14 に
  PROGRESS.md を壊した）。書き換えは Edit ツールか Python で行う

## 手順ページのノードグラフ

- 撮り方: Houdini で `bridge_server.start()` → Houdini 同梱の python で `guide_ui_capture.py all`（全体）と `guide_parm_capture.py all`（各段）
- 段ごとの `ui_parm` で見せたい項目を指定できる（そのタブ・折りたたみ欄を開き、パラメータ欄をそこまでスクロールして撮る）。`ui_node` は / から書けば /obj や /out のノードも撮れる
- **撮影窓は「Console を含まない一番大きい窓」を選ぶ**（`capture_window.ps1`）。MainWindowHandle だと Houdini Console の小窓を撮ってしまった
- 段とノードの対応は `step.node`（型名かパラメータ名）。同名パラメータが複数あるときは `step.ui_node` で指定
- パラメータ名の段は、そのパラメータのあるタブを開いてから撮る

### 以前のメモ

- 実物の Houdini 画面で撮りたい（`guide_ui_capture.py all`）。道具はできている
- ただし **PC の画面に Houdini が映っていないと、古い画のまま撮れてしまう**。
  タイトルバーだけ新しくなり、パネルは前のシーンのまま写る
- 本人が PC の前にいて Houdini が見えている状態で実行すること

## Apprentice の制限（実験058で全部試した）

- **FBX は書き出せない**（「FBX export is not supported in Houdini Apprentice.」）
- **glTF と Alembic も不可**（「only supported in Houdini Core and Houdini FX versions.」）
- **USD は LOP 経由なら書けるが、拡張子が `.usdnc`（非商用）に変えられる**
- 通るのは `hou.Geometry.saveToFile` での直接保存：
  `bgeo.sc` / `geo` / `obj` / `ply` / `stl`
- bgeo.sc と geo は完全に往復（ずれ 0、`name` も残る）。
  obj と ply は位置だけ戻り `name` は消える。stl は点が減る
- **Unreal へ渡すにはライセンスを上げるしかない。工夫では回避できない**

## 守ること（破ると後で辻褄が合わなくなる）

- 実測を正とする。推測を書かない
- **実験では結果と一緒に、かかった時間も必ず記録する**（1回目は捨てる／新しいノードの初回を測る）。速さの結論が出たら `speed_tips.json` に足す
- 数字はすべて実行して得た値。手打ちの数字を残さない
- 訂正したら元の記事の冒頭にも注記を出す
- 新しい用語が出たら `glossary.json` に足して `link_terms.py` を回す
- 変更したら `python publish.py "説明"` でビルド・コミット・プッシュまで行う

## 実験071〜078（2026-09-19）で分かったことの要点

- 071 回る台：回転は台の 0.98〜1.00 倍で伝わる。滑り出す境目は ω²r ≒ 一定（式の形どおり）だが、間隔 0.12 では実効の摩擦 0.3〜0.4
- 072 たわむ板：Deforming+computevelocity は板の速さを渡す（高さは式の 30〜76%）。Rigid はたわみを平均の上下動に。Deforming で cv 切りはすり抜け。Deforming は Rigid の 2.3倍重い
- 073 Liquid の暴れ：摩擦 0.25〜1.0 は全部 F28〜46 から数粒が飛ぶ（068 の「0.25 は静か」は40フレームで止めていたため → 068 に注記済み）。摩擦 0 と 1.5 以上は静か。substep 8 で消える（4.7〜6.3倍）
- 074 sticky：地面とコライダで同じく 1〜2 で頭打ち（5 以上で6桁一致）。sticky 1000 でも天井から落ちる
- 075 摩擦の上限：粒を細かくすると上がる（0.12 で 0.34 → 0.04 で 0.79）。substep では1ミリも動かない（071 に注記済み）
- 076 滑る塊・動く板：細かくすると逆に式から離れる（塊が潰れる）。摩擦 0.25 だけはどの細かさでも式どおり
- 077 測り方：1粒ずつ position() を読むと計算と同じだけ重い（15,591粒で 82ms/フレーム）。numpy.frombuffer なら 0.11ms。`hou_tools.point_array()` を追加
- 078 Liquid の暴れと細かさ：細かくしても消えない（飛ぶ高さだけ下がる）。止めるのは substep
- 記事は `examples/make_entry.py NNN log_fx チップ パス 一言` でレポート JSON から生成（数字の写し間違いを防ぐ）。後から条件や訂正が付いたら JSON の `callout` に書く
- 折れ線グラフは `examples/pil_chart.py`（matplotlib が無いため）

## 実験079〜082（2026-09-19・手順ページ向き）

- 079 火花：Life Variance は ±幅に一様、初速の Variance は「その半径の球の中」に一様、popdrag は速さの2乗（ずれ 0.0000）。DOP の中はソルバに表示フラグ必須。All Points だと Impulse Count が効かない → 手順「火花を散らす」
- 080 雨：Bounce は速さの比（高さは約 Bounce²）。地面と粒の Bounce は掛け算。Response Die で消える。merge に表示フラグ、dopimport の対象は粒の名前 → 手順「雨を降らせて地面で跳ねさせる」
- 081 旗：風速 2 までほぼ垂れたまま。持ち上がりは「抵抗 × 風速²」でほぼそろう。揺れるものは一定時間の平均で測る → 手順「旗を風になびかせる」
- 082 道に沿って並べる：resample の Length は上限（全体を等分）。坂では N だけだと最大49度倒れ、up を足すと0度。Pack and Instance で 1万個 714ms→2.9ms・1878MB→4MB → 手順「道に沿って物を並べる」
- 手順は 29本
- **Claude 版は1つの版に置けるファイルが 512 まで。** 概要ページ（home）が上限に近いので、`build_site.py` の `same_image()` で中身が同じパラメータ画面は1枚にまとめている。更新時は使わなくなった画像を `files` の null で外す
- シェルのヒアドキュメントから Python に `
` を含む文字列を書かせると、本物の改行になって壊れる。文字列の書き換えは Edit ツールで行う

## 実験083〜090（2026-09-20）

- 083 文字の看板：font の LOD は点の数に比例（大きさは不変）。polyextrude の Distance はそのまま奥行き。polybevel は 0.08 以上で形が変わらない（面積 1.616047 で一致）→ 手順「看板の文字を立体にする」
- 084 ドミノ：**初速はプリミティブでなく点に付ける**（packed に v@v は効かない）。間隔は高さの 0.25〜1.03倍。倒れる速さは1秒に 2〜10枚 → 手順「ドミノを倒す」
- 085 壁：拘束の強さ（Primary Strength）の窓は狭い。既定（1000）では壊れず、100 では自重で崩れる。500 前後が使える。破片20倍でも解く時間は4.4倍 → 手順「壁を崩す」
- 086 舞う葉：popwind の Noise。Amplitude で散らばりと速さが増える（0→4 で 0.76→1.47、1.88→3.59）。小さい乱れ（0.5〜1）は差が出ない → 手順「葉を風で舞わせる」
- 087 ターンテーブル：形は物を回してもカメラを回しても同じ（シルエット 0.01ポイント一致）。違うのは明るさだけ（186→51）→ 手順「ターンテーブルで1周見せる」
- 088 発光：Emission を上げても文字は頭打ち（16→64 で +1.8%）。増えるのはこぼれ光と時間（3.8→12.4秒）。emitillum を切ると3倍速い → 手順「文字を光らせる」
- 089 落ち葉：積もるかは Response で決まる（Stick で 180/400枚が停止、既定 20、Slide 0）。摩擦では止まらない → 手順「落ち葉を地面に積もらせる」
- 090 点検：7件を測り直して全部一致。訂正は 068←073 と 071←075 の2件。「黙って無視される設定」は8件に
- 手順は 36本。効率化の項目は 28
- **hython はレンダのあとにシーンを作り直すと落ちる**（OpenGL Fatal Error か無応答）。実験083・087で再現。1プロセス1レンダか、作り直さない組み方にする
- **Claude 版は1つの版に置けるファイルが 512 まで。** 実験ポップアップの図は Claude 版だけ1枚に絞る（`build_site.render_exp_data` の slim）。差分は `git show <前回publishのコミット>:site/home.html` と今の参照を比べて出す
