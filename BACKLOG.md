# Houdini 実験バックログ

重点はエフェクトとプロシージャルモデリング。1実験ごとにレポートPDFを作り、
テーマ単位で束ねて Gemini Notebook「Houdini 操作ガイド」のソースを差し替える
（方針は `SOURCE_STRATEGY.md`）。

完了した実験の中身はサイトの実験ログに全文がある。ここには「これからやること」を残す。

## 完了（001〜018）

001 最小構成 / 002 subdivide の回数 / 003 処理順序 / 004 mountain のパラメータ /
005 ノイズの種類14通り / 006 height・gain・bias / 007 平面の地形 /
008 scatter と copy to points / 009 VEX / 010 for-each と VEX の比較 /
011 crease / 012 polyextrude と boolean / 013 時間軸 / 014 RBD 破壊 /
015 Vellum クロス / 016 Pyro 煙 / 017 FLIP（保留）/ 018 Karma レンダリング

## 次にやること

- [ ] **019 FLIP の再挑戦** — 017で4通りの配線を試して全部同じエラーだった。
  付属ヘルプ（`nodes.zip`）で flipsolver の入力の説明を読むか、
  GUI のシェルフツールに FLIP を作らせて `.hipnc` から構造を読み取る
- [ ] **020 POP パーティクル** — SOPには0種、DOPに67種。DOPネットワークを組む必要がある
- [ ] **021 構造のある煙を作る** — 018で分かったこと。いまの煙はほぼ一様な球で、
  レンダラーの差を見せる題材として弱い。乱流を入れて形のある煙にしてから撮り直す

## ゆくゆく取り組むもの（ユーザーからの希望）

- [ ] **APEX とリグ** — Houdini 20以降のリギングの仕組み。キャラクターを動かす側。
  これまでの実験はすべてジオメトリとシミュレーションなので、まったく別の系統になる
- [ ] **テクスチャの生成** — プロシージャルにテクスチャを作る。COP や Karma のマテリアル側
- [ ] **MetaHuman 系（Groom 中心）** — うちの Houdini に入っているので実機で確かめられる。
  髪や毛の生成・シミュレーション。Vellum の知識（015）が下地になるはず

## ツール側

- [x] 複数フレームの書き出し（`render_sequence`）
- [x] コンタクトシートと GIF（`sequence_report.py`）
- [x] 同一カメラでの比較レンダ（`bbox_union` + `frame_bbox`）
- [x] 変更したパラメータの自動抽出（`_changed_parms`）
- [x] GUI の画面写真（`gui_capture.py` + `bridge_server.py`）
- [x] 用語の自動リンク（`link_terms.py`。タグを差し込むだけで本文は変えない）
- [x] Karma でのレンダリング（018で経路を確立）
- [ ] **シミュレーションのキャッシュ** — file cache で中間結果を保存する。
  018で同じ煙を何十回も描き直したので、そろそろ効いてくる
- [ ] **Karma のレンダ設定を hou_tools に入れる** — 018で調べた設定
  （denoiser off、varianceaa_thresh、露出）を関数にまとめる

## 実験の進め方（003で確立し、以降で足した型）

1. **基準形を作る。** 何もしていない状態を並べて置く。これがないと変化を測れない
2. **同一カメラでレンダする。** 各変種を自動フレーミングすると大きさの違いが消える。
   018でこれを一度破って、比較にならない数字を出した
3. **基準形と1対1で対応づけられる量を測る。** 点ごとの差が最も素直。
   表面積のような「全体をひとつにまとめた量」は大きさと形が混ざるので不適
4. **仮説を立てたら、それを否定できる条件を作って試す。**
   002では「縮む原因は平滑化」の検証に、平滑化しない Bilinear を使った
5. **ツマミが効いていることを確かめてから測る。** 009では処理時間が、018ではサンプル数が、
   指定しても実際には変わっていなかった。時間と結果の**両方**が動いて初めて本物
6. **失敗した測り方も残す。** なぜ不適切だったかが次の実験の役に立つ
