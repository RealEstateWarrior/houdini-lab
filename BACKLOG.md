# Houdini 実験バックログ

重点はエフェクトとプロシージャルモデリング。1実験ごとにレポートPDFを作り、
テーマ単位で束ねて Gemini Notebook「Houdini 操作ガイド」のソースを差し替える
（方針は `SOURCE_STRATEGY.md`）。

## 完了

- [x] **001 box_mountain** — box → mountain → subdivide → color の最小構成。
  `examples/box_mountain.py`
- [x] **002 subdivide の回数比較** — 0〜6回。面数は正確に4倍、点数は常に面数+2（オイラーの多面体定理）。
  1回目だけ縮まない理由、縮みの原因が平滑化であること、Mantra-Compatible が極限曲面を
  直接評価していることを実測で確定。`examples/002_subdivide_iterations.py`
- [x] **003 処理順序の比較** — mountain と subdivide の順序、および elementsize と点数の役割分担。
  「点数は解像度、elementsize は振幅」を6通りの比較で確定。
  表面積を指標に選んだ失敗も記録した。`examples/003_order_matters.py`

## プロシージャルモデリング

- [ ] **004 mountain のパラメータを切り分ける** — roughness と octaves が振幅と細かさの
  どちらに効くのかを、003と同じ「基準形との点ごとの差」で分離する
- [ ] **005 grid から地形** — 平面でも003と同じ結論になるか。行列数とノイズ設定の関係
- [ ] **006 crease で角を保持** — 分割しても立方体らしさを残せるか（002からの持ち越し）
- [ ] **007 scatter + copy to points** — プロシージャルの中核パターン。pscale で個別にサイズ変更
- [ ] **008 for-each ループ** — プリミティブ単位の繰り返し。イテレーション番号で結果を変える
- [ ] **009 attribute wrangle (VEX) 入門** — `@P` / `@N` / `@ptnum`
- [ ] **010 polyextrude / boolean** — 面の押し出しとブーリアンで簡単な建物

## エフェクト

- [ ] **011 POP パーティクル基本** — source と solver。重力と寿命
- [ ] **012 RBD 破壊** — voronoi fracture からの剛体シミュレーション
- [ ] **013 Vellum クロス** — 布のシミュレーション
- [ ] **014 Pyro と FLIP** — ボリュームと液体

## ツール側の未対応事項（エフェクトに入る前に必要）

- [ ] **複数フレームの書き出し** — 現在 `render_preview` は1フレームのみ。
  シミュレーションは時間変化が本体なので、フレーム範囲を回した連番が必要
- [ ] **シミュレーションのキャッシュ** — file cache で中間結果を保存する
- [ ] **実行時間の記録** — シミュレーションは重いので、どの実験がどれだけかかったかを残す

## 完了したツール側の対応

- [x] **複数画像を並べたレポート** — `report_pdf.py` の `comparisons` ブロック。002・003で使用
- [x] **同一カメラでの比較レンダ** — `hou_tools.bbox_union()` + `render_preview(frame_bbox=...)`。
  各変種を自動フレーミングすると大きさの違いが消えるため必須
- [x] **変更したパラメータの自動抽出** — `hou_tools._changed_parms()`。
  デフォルトから動かした値だけを拾うので、レポートが設定値の羅列で埋まらない
- [x] **用語集** — `glossary.json` を単一の情報源に、辞典ページ・ポップアップ・Notebook用PDFを生成
- [x] **サイト分割** — ホーム / 実験ログ / 用語辞典 / 参考リンク集 の4ページ構成

## 実験の進め方（003で確立した型）

1. **基準形を作る。** 何もしていない状態を並べて置く。これがないと変化を測れない
2. **同一カメラでレンダする。** 各変種を自動フレーミングすると大きさの違いが消える
3. **基準形と1対1で対応づけられる量を測る。** 点ごとの差が最も素直。
   表面積のような「全体をひとつにまとめた量」は大きさと形が混ざるので不適
4. **仮説を立てたら、それを否定できる条件を作って試す。**
   002では「縮む原因は平滑化」の検証に、平滑化しない Bilinear を使った
5. **失敗した測り方も残す。** なぜ不適切だったかが次の実験の役に立つ
