# これからの計画（2026-09-20 決め）

## 目標（10月まで）

| 項目 | いま | 目標 | 足りない分 |
|---|---|---|---|
| 実験 | 90 | 500 | 410 |
| 解説するノード | 48 | 600 | 552 |
| 用語 | 119 | 400 | 281 |
| 実践（旧「手順」） | 36 | 250 | 214 |
| 制作（新設） | 0 | 命令が来た分だけ | — |

### 実測した今までの速さ

最初のコミットが 2026-09-13、いまが 2026-09-20。**8日**で上の「いま」まで来た。

| 項目 | 1日あたり（実測） | 9月末までに必要 | 10月末までに必要 |
|---|---|---|---|
| 実験 | 11.3 | 41.0 | 10.0 |
| ノード | 6.0 | 55.2 | 13.5 |
| 用語 | 14.9 | 28.1 | 6.9 |
| 実践 | 4.5 | 21.4 | 5.2 |

**9月末（あと10日）では届かない。10月末（あと41日）なら、ノードの作り方を変えれば届く。**
ノードだけが 2.25 倍必要。他は今の速さで足りる。

## つまずく所は速さではなく2つ

### 1. Claude 版のファイル数（512の壁）

いまの概要ページは 501/512。実践が 250 本になると、画像だけで 1000 枚を超える。
実測: 概要ページ1枚に画像タグが **577 個**、スマホ幅でページの高さ **8517px**。

対策（`assets` 置き場を使う）:
- Artifact には published files（512枚まで）とは別に「アセット置き場」がある。
  1ファイル 20MiB まで、容量は artifact ごとの枠。`capabilities: {assets: {}}` を宣言し、
  publish に `asset: true` で上げると、返ってきた URL を `<img src>` にそのまま使える。
- 注意: `assets` を宣言したページは組織内限定になり、公開リンクでは配れない。
  公開は GitHub Pages 版が担うので、Claude 版が組織内限定になるのは問題ない。
- まだ確かめていない: 枠の総容量、1つの artifact に上げられる個数。最初に少数で試す。

### 2. トークン（利用枠）

8日で 90 実験を回した速さは、枠にぶつかりながらの速さだった。数を3〜5倍にするなら、
1件あたりの消費を下げるしかない。下げ方（上から効く順）:

1. **実践ページも JSON から組み立てる。** いま実験記事は `examples/make_entry.py` が
   レポート JSON から作っている。実践も同じにして、HTML を手で書かない。
2. **掃き出し（sweep）ハーネスを作る。** 「値を並べて測って表と図にする」までを共通化し、
   1実験のスクリプトで手書きするのは測る所だけにする。
3. **ノードは hython から種を作る。** `hou.nodeType(...).parmTemplates()` で
   ラベルと既定値を吐かせ、私が足すのは1〜2行の説明だけにする。600本はこれで初めて届く。
   （UI 名を実測で確かめる規則も、これで自動的に守れる）
4. **画像は1実験2枚まで。** 比較図1枚と結果1枚。ファイル数もトークンも半分になる。
5. **大きいファイルは grep で行を絞って読む。** 全文を開かない。STATE.md だけを起点にする。
6. **hython の起動は1.0秒**（実測3回: 1.16 / 0.98 / 0.99）。**まとめて1プロセスで回す工夫は
   効かない**ので、そこに時間を使わない。

## 名前

- 「手順」→ **実践**。画面に出る言葉だけ変える。中のファイル名（`guides.json`、`guide_*.png`）は
  そのままにする。300枚以上の画像を作り直す価値がないため。
- 新設は **制作**。実践との違いをはっきりさせる:
  - **実践** … 誰がやっても同じ結果になるレシピ
  - **制作** … 頼まれた一点物の記録。概要 / 完成物 / 手順 / 過程（つまずきと直し方） / 使ったノード / かかった時間
- 「制作」以外の候補: 依頼、作例、受注。短くて実践と並ぶので「制作」を推す。

## 相談チャット AI

- **Claude 版はすでにチャットになっている。** 直近8往復を覚え、「止める」「会話を消す」も付いている。
  2026-09-20 に「ベータ」の印を付けた。
- Gemini を使うなら、鍵の置き場が問題。GitHub Pages は静的配信なので、鍵をページに書くと誰でも盗める。
  - 案A: Claude 版だけ AI を持つ（いまの形）。追加費用ゼロ。
  - 案B: 中継を1つ立てて鍵をそこに置く（Cloudflare Workers の無料枠など）。アカウント作成はユーザー。
  - 案C: 閲覧者が自分の鍵を入れる欄を作る。
- トークンを減らす手: いまは質問ごとに上位10件の記録を添えている。4件に減らし、抜粋も短くする。
- まだ確かめていない: Gemini の無料枠の現在の上限。

## Unreal Engine

実験501番以降の題材にする。測れることが多い:
書き出しにかかる時間、ファイルの大きさ、アトリビュートが生き残るか（Alembic / USD / FBX の比較）、
Houdini Engine for Unreal で何が動いて何が動かないか。
UE が入っているかは未確認。入っていなければ、そこから。

## サイトのデザイン見直し

- 済み: スマホの検索を右へ（実測 375px 幅で 281〜317px、ハンバーガーは 323〜359px）
- 提案1（効く順に一番上）: **一覧を索引表にする。** 概要ページに画像タグが 577 個、
  スマホで 8517px ある。実践が 250 本になると開けない。既定を行の高さ 44px の表＋絞り込みにし、
  大きな図版は「今月の見どころ」数本だけに残す。
- 提案2: 区切りの余白を `clamp(72px, 10vw, 120px)` に。今は 72px 固定。
  同じ種類のサイト24件の実測では、一番大きな段は中央値 96px（inspo MCP 調べ）。
- 提案3: 3つの中身を色で区別する（実験=青 `--flag`、実践=橙、制作=もう1色）。
  リンクの青はそのまま。
- 提案4: 深い書庫向けの「索引フッター」（番号付き・等幅・下線なし）を各ページの足に置く。

## 次の決めごと

1. 「10月まで」は 9月末か 10月末か
2. 新設の名前を「制作」でよいか
3. チャット AI は 案A / 案B / 案C のどれか
4. UE は入っているか
